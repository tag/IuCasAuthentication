language: php
php:
  - '5.6'
  - '7.1'

env:
  global:
    - COMPOSER_ARGS="--no-interaction --no-progress --no-suggest"

cache:
  directories:
    - $HOME/.composer/cache

install:
  travis_retry composer update $COMPOSER_ARGS

script:
  - composer test
  - composer phpcs
